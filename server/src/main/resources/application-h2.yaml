# H2数据库配置（默认）
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:oauth;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
